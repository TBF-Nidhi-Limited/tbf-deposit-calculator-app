import React from 'react';
import Combiner from '../../Combiner';
import HomeConfig from './HomeConfig';


const Home = (props) => {
    return (
        <div>
     <HomeConfig/>
        </div>
    );
}

export default Combiner(Home);